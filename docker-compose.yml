

services:

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: sano
      MONGO_INITDB_ROOT_PASSWORD: sano
      MONGO_INITDB_DATABASE: sano-db